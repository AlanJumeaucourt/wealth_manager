# Production API (Bun + Elysia + SQLite). Build context: `backend/`.
FROM oven/bun:1

WORKDIR /app

COPY package.json bun.lockb ./
RUN bun install && test -d node_modules/@elysiajs/cors

COPY . .

ENV NODE_ENV=production
EXPOSE 5000

CMD ["bun", "run", "start"]
