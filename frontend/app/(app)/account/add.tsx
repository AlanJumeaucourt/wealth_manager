import { Text, View } from "react-native";

export default function AccountAddPlaceholder() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Add account</Text>
    </View>
  );
}
