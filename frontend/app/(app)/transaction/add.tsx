import { Text, View } from "react-native";

export default function TransactionAddPlaceholder() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Add transaction</Text>
    </View>
  );
}
