import type { Bank, BankCreateBody, BankQueryParams } from "@/api/edenDerivedTypes";

/** Bank types — from Eden (`src/api/edenDerivedTypes.ts`). */
export type { Bank, BankCreateBody, BankQueryParams };
